const GuestLayout = ({ guestchildern }) => {
  return (
    <>
      <div className="min-h-screen flex items-center justify-center p-4">
        <div class="w-full max-w-md">{guestchildern}</div>
      </div>
    </>
  );
};

export default GuestLayout;
